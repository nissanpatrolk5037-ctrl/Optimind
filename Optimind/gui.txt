<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Jarvis UI</title>

<style>
body {
    margin: 0;
    height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    position: relative;
    color: #fff;

    font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;

    /* Cool dotted UI background */
    background-color: #0a2a5b;
    background-image:
        radial-gradient(
            rgba(255,255,255,0.08) 0.8px,
            transparent 0.9px
        );
    background-size: 22px 22px;
}


  /* JARVIS Circle */
  .circle {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    border: 4px solid #1e90ff;
    display: flex;
    justify-content: center;
    align-items: center;
    font-weight: 600;
    font-size: 20px;
    position: relative;
    animation: pulse 2s infinite ease-in-out;
    box-shadow: 0 0 30px #1e90ff;
    transform: scale(0.85);
    margin-top: 50px;
  }

  @keyframes pulse {
    0% { transform: scale(0.85); box-shadow: 0 0 18px #1e90ff; }
    50% { transform: scale(1); box-shadow: 0 0 32px #1e90ff; }
    100% { transform: scale(0.85); box-shadow: 0 0 18px #1e90ff; }
  }

  .circle::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 180px;
    height: 180px;
    border: 2px solid rgba(30,144,255,0.45);
    border-radius: 50%;
    transform: translate(-50%, -50%);
    animation: rotate 3s linear infinite;
  }

  @keyframes rotate {
    from { transform: translate(-50%, -50%) rotate(0deg); }
    to { transform: translate(-50%, -50%) rotate(360deg); }
  }

  /* EXTRA-THIN INPUT BOX */
  .input-box {
    position: absolute;
    bottom: 40px;
    width: 280px;
    max-width: 80%;
    padding: 6px 14px;              /* thinner */
    border-radius: 22px;
    border: 1.5px solid #1e90ff;    /* slimmer border */
    background: rgba(255,255,255,0.04);
    color: #fff;
    font-size: 14px;
    font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    outline: none;
    box-shadow: 0 0 10px rgba(30,144,255,0.25);
    text-align: center;
    transition: all 0.25s ease;
  }

  .input-box::placeholder {
    color: rgba(255,255,255,0.55);
    font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
  }

  .input-box:focus {
    background: rgba(255,255,255,0.08);
    box-shadow: 0 0 18px rgba(30,144,255,0.55);
    border-color: #1e90ff;
  }

  /* Small, borderless fallback text */
  .fallback-response {
    position: absolute;
    bottom: 10px;
    left: 10px;
    font-size: 12px;
    color: rgba(255,255,255,0);
    font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    transition: color 0.6s ease, text-shadow 0.6s ease;
  }

  .fallback-response.show {
    color: rgba(255,255,255,0.85);
    text-shadow: 0 0 6px #1e90ff, 0 0 12px rgba(30,144,255,0.4);
  }
</style>

<script src="qrc:///qtwebchannel/qwebchannel.js"></script>
</head>
<body>

<div class="circle">A.I.</div>

<input
  type="text"
  id="input"
  class="input-box"
  placeholder="Type a command..."
>

<div class="fallback-response" id="fallback">Hello There!</div>

<script>
  let bridge = null;
  new QWebChannel(qt.webChannelTransport, channel => {
    bridge = channel.objects.bridge;
  });

  const input = document.getElementById("input");
  const fallback = document.getElementById("fallback");

  input.addEventListener("keypress", e => {
    if (e.key === "Enter" && bridge) {
      const prompt = input.value.trim();
      if (!prompt) return;

      fallback.classList.remove("show");
      void fallback.offsetWidth;

      bridge.get_response(prompt).then(response => {
          fallback.textContent = response;
          fallback.classList.add("show");
      });


      input.value = "";
    }
  });
</script>

</body>
</html>
